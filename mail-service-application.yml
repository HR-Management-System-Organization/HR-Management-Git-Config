server:
  port: 7075

rabbitmq:
  auth-exchange: auth-exchange
  register-binding-key: register-key
  register-queue: register-queue
  activation-binding-key: activation-key
  activation-queue: queue-activation
  mail-queue: mail-queue
  mail-binding-key: mail-binding-key

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: user
  mail:
    host: smtp.gmail.com
    port: 587
    username: alperensertoglu1@gmail.com
    password: sqvq amax jldi nnso
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true